document.querySelector('[data-timer-session-container]').innerHTML =
  ("<%= j(render 'timer_sessions/timer_container', timer_session: nil) %>")

window.ActionBinder.bind();
window.ActionBinder.hoursTracker().endTicker();
