<% @current_timer_session.validate %>

document.querySelector('[data-timer-session-container]').innerHTML =
  ("<%= j(render 'timer_sessions/timer_container',
          timer_session: @current_timer_session) %>")
