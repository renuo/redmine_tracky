<% @timer_session.validate if @timer_session.errors.empty? %>
$('#ajax-modal').html(
  ("<%= j render 'edit_modal', timer_session: @timer_session %>")
);

showModal('ajax-modal', '800px', '<%= t('timer_sessions.edit.title') %>');
$('#ajax-modal').addClass('new-watcher');
